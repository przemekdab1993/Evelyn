{% extends 'base.html.twig' %}

{% block title %}Hello DocController!{% endblock %}

{% block body %}

    <div class="box-content">
        <h1>Lista dokumentów</h1>
    </div>

    <div class="box-content-2">
        <ul class="blog-list">
            {% for doc in pagerDocList %}
                {{  include('doc/list/_item.html.twig',[]) }}
            {% endfor %}
        </ul>
    </div>
    <div class="box-content-2">
        {{ pagerfanta(pagerDocList) }}
    </div>
{% endblock %}
